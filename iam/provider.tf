####################################################################
# Provider 설정 - 어떤 클라우드를 사용할지 정의
####################################################################

# provider = 테라폼이 어떤 서비스(AWS, GCP 등)와 통신할지 설정
# - var.aws_region: variables.tf에서 선언한 변수 값 사용
provider "aws" {
  region = var.aws_region
}
